<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}Yummy!{{else}}Eat Me!{{/if}}
	</button>
	<button class="delete" data-id= "{{id}}"> Delete Burger! </button>
</li>
